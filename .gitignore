# Python

# Node / Frontend
node_modules/
dist/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build artifacts

# Model weights (track explicitly if needed)

# Dataset raw images can be large; keep labels & yaml
DATASET/train/
DATASET/valid/

# YOLO runs (already committed one set; ignore future unless curated)

# OS
.DS_Store
Thumbs.db

# Notebooks checkpoints

